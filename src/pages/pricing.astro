---
import Navbar from '../components/Navbar.astro';
import Section from '../components/Section.astro';
import Footer from "../components/Footer.astro";
import AddSpace from "../components/AddSpace.astro";
import Pricing from "../components/Pricing.astro";
import Base from "../layouts/Base.astro";
import { footerLinks } from "../data/footerLinks.js";
import { pricingData } from "../data/pricingData.js";


const menuItems = [];

const {
  branding,
  columns,
  advancedTitle,
  advancedColumns,
  social: socialLinks,
  bottom,
} = footerLinks;
---
<Base title="Pricing — Kudobuzz">
  <Navbar menuItems={menuItems} />
 <AddSpace color="transparent" />

  <Section
    tag="Pricing"
    title="Choose a plan and "
    titlehighlight="grow your business effortlessly"
    layout="center"
    colorScheme="white"
    padding="0px"
  />

  <Pricing
    tabs={pricingData.tabs}
    plansByTab={pricingData.plansByTab}
    modalContent={pricingData.modalContent}
/>

  <AddSpace color="transparent" />
  <AddSpace color="transparent" />
  <AddSpace color="transparent" />

<Footer
  branding={branding}
  columns={columns}
  advancedTitle={advancedTitle}
  advancedColumns={advancedColumns}
  socialLinks={socialLinks}
  bottom={bottom}
/>
</Base>
